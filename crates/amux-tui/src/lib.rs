//! `amux-tui` — the ratatui client: a projection of daemon state. Renders the sidebar, the
//! main window, and the floating mini-terminals; routes focus and input. See
//! `docs/DESIGN.md` §7.
//!
//! Populated from Phase 0.5. The rendering approach is proven first by `examples/spike.rs`.
